machine:
  java:
    version: oraclejdk8

test:
  override:
    - mvn install
  post:
    - "ls -la"
    - pwd
    - cp -R /home/ubuntu/netty-tcnative/openssl-static/target/* $CIRCLE_ARTIFACTS

general:
  branches:
    only:
      - 1.1.33_ossl_static
