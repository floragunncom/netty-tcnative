machine:
  java:
    version: oraclejdk8

test:
  override:
    - mvn install
  post:
    - cp -R /home/ubuntu/netty-tcnative/openssl-static/target/netty-tcnative-openssl-static-*-linux-x86_64.jar $CIRCLE_ARTIFACTS

general:
  branches:
    only:
      - 1.1.33_ossl_static
